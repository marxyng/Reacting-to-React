import React from 'react';
import ReactDOM from 'react-dom';
import App from './components/app';

ReactDOM.render(<App name="Rednose"/>, document.getElementById('root'));

 /*const Name = (props) => {
    return (
         'Big Marco'
     );
};
export default Name;
*/
